---
layout: base
---
<article>
<h1> {{ title }} </h1>
  {% if thumbnail %}
    <img class="img-thumbnail-centered" src="{{ thumbnail }}/public" alt="{{ title }}">
  {% endif %}
 {{ content }}

</article>
